{#{% extends 'base.html.twig' %}

{% block title %}Medecin{% endblock %}

{% block body %}
    <h1>Medecin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ medecin.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ medecin.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ medecin.prenom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ medecin.adresse }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ medecin.tel }}</td>
            </tr>
            <tr>
                <th>HorairesConsultation</th>
                <td>{{ medecin.horairesConsultation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_medecin_index') }}">back to list</a>

    <a href="{{ path('app_medecin_edit', {'id': medecin.id}) }}">edit</a>

    {{ include('medecin/_delete_form.html.twig') }}
{% endblock %}
#}
{% extends 'base.html.twig' %}
{% block html %} 
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

        {% block css %}
          {{ parent() }}
        {% endblock %}

</head>
{% block title %}Evenement{% endblock %}

{##############################################################}
{#########################BODY#################################}
{% block body %}
<div class="site-wrap">

  {% block navbar %}
    {{ parent() }}
  {% endblock %}

    <div class="bg-light py-3">
      <div class="container">
        <div class="row">
          <div class="col-md-12 mb-0">
            <a href="index.html">Home</a> <span class="mx-2 mb-0">/</span> 
            <strong class="text-black">Cart</strong>
          </div>
        </div>
      </div>
    </div>

{##############################################################}
{#########################DETAILS###############################}
    <div class="title-section text-center col-12 mt-5 mb-3">
        <h2>Détails <strong class="text-primary">Medecin</strong></h2>
    </div>
    <div class="col-md-12">
        <div class="site-blocks-table">
            <table class="table table-bordered">
                <tbody>
            <tr>
                <th>Id</th>
                <td>{{ medecin.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ medecin.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ medecin.prenom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ medecin.adresse }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ medecin.tel }}</td>
            </tr>
            <tr>
                <th>HorairesConsultation</th>
                <td>{{ medecin.horairesConsultation }}</td>
            </tr>
            </table>
        </div>
    </div>
        <div class="container">
            <div class="row mb-5 mt-3">
                <div class="col-md-4">
                    <a class="btn btn-primary btn-md btn-block" href="{{ path('app_medecin_edit', {'id': medecin.id}) }}">Modifier</a>
                </div>
                <div class="col-md-4 mb-3 mb-md-0">
                    <a class="btn btn-outline-primary btn-md btn-block" href="{{ path('app_medecin_index') }}">Retourner</a>
                </div>
                <div class="col-md-4 mb-3 mb-md-0">
                    {{ include('medecin/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    {####################################################################}
    {############################FOOTER##################################}
    {% block footer %}  
        {{ parent() }}
    {% endblock %}
    </div>  
         
    {###################################################################################}

{% endblock %}
</html>
{% endblock %}