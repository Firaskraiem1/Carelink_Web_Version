{{ form_start(form,{'attr':{'novalidate':'novalidate'}}) }}
    
    <div class="form-group">
        {{ form_label(form.titre, "Titre") }}
        {{ form_widget(form.titre, {'attr': {'class': 'form-control'}}) }}
        {{ form_errors(form.titre) }}
    </div>

    <div class="form-group">
        {{ form_label(form.contenu, "Contenu de la reclamation") }}
        {{ form_widget(form.contenu, {'attr': {'class': 'form-control'}}) }}
        {{ form_errors(form.contenu) }}
    </div>


    <div class="container">
        <div class="row mb-3">
            <div class="col-md-6 mb-3 mb-md-0">
                <button class="btn btn-primary btn-md btn-block">{{ button_label|default('Envoyer reclamation') }}</button>
            </div>
            <div class="col-md-6">
                <a class="btn btn-outline-primary btn-md btn-block" href="{{ path('app_evenement_historique_index') }}">Annuler</a>
            </div>
        </div>
    </div>
{{ form_end(form) }}